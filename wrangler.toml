name = "ai-style-writer"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# pages_build_output_dir je pouze pro lokální vývoj (wrangler pages dev)
# Při deployi přes Cloudflare dashboard se toto pole NENASTAVUJE
